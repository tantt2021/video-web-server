import { AllExceptionFilterFilter } from './all-exception.filter';

describe('AllExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new AllExceptionFilterFilter()).toBeDefined();
  });
});
